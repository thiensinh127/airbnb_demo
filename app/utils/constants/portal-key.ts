const portalClasses = {
  calendar: ".calendar",
};

const portalIds = {
  popover: "popover-container",
  select: "select-container",
  dropdown: "dropdown-container",
  debounceSelect: "debounce-select-container",
  locationDropdown: "location-dropdown-container",
  treeDropdown: "tree-dropdown-container",
};

export { portalClasses, portalIds };
